🗓 DAY 1 — Spring Boot Basics & Bootstrapping

❓ What is Spring Boot?
✅ Answer:
Spring Boot is an opinionated framework that simplifies Spring application development by providing auto-configuration, embedded servers, and production-ready features.

❓ What problems does Spring Boot solve?
✅ Answer:
- Eliminates boilerplate configuration
- No need for external application servers
- Simplifies dependency management
- Faster application startup

❓ What is @SpringBootApplication?
✅ Answer:
It is a composite annotation that includes:
- @Configuration
- @EnableAutoConfiguration
- @ComponentScan

❓ What happens when a Spring Boot application starts?
“SpringApplication.run() bootstraps the application, creates the ApplicationContext, performs classpath scanning, applies auto-configurations, and starts the embedded server.”✅ Answer:
- Application context is created
- Auto-configuration is applied
- Beans are scanned and registered
- Embedded server (Tomcat) starts
- Application is ready to accept requests

❓ What is embedded Tomcat?
✅ Answer:
An embedded servlet container bundled with Spring Boot, allowing applications to run as standalone JARs without deploying to external servers.

❓ Difference between Spring and Spring Boot?
✅ Answer:
| Feature | Spring | Spring Boot |
|---------|--------|------------|
| Configuration | Requires manual config | Auto-configuration |
| Server | External server needed | Embedded server |
| Approach | XML-heavy | Java & annotation-based |

❓ What is Spring Initializr?
✅ Answer:
A tool to generate Spring Boot projects with predefined dependencies and project structure.

❓ How does Spring Boot know which packages to scan?
✅ Answer:
By default, it scans the package of the main class and all its sub-packages.

---

🗓 DAY 2 — Layered Architecture, DTO & Entity

❓ What is layered architecture in Spring Boot?
✅ Answer:
A design pattern that separates responsibilities into layers:
- Controller Layer (HTTP handling)
- Service Layer (Business logic)
- Repository Layer (Data access)

❓ Why do we need a Service layer?
✅ Answer:
To encapsulate business logic and prevent controllers from becoming fat and unmaintainable.

❓ Why should controllers not contain business logic?
✅ Answer:
Because controllers should handle HTTP concerns only and delegate business logic to services for better maintainability and testability.

❓ What is a DTO?
✅ Answer:
A Data Transfer Object used to transfer data between layers, especially between client and server, without exposing internal domain models.

❓ Why should we not expose Entity directly in APIs?
✅ Answer:
- Security risks (over-posting attacks)
- Tight coupling with database schema
- API breaks when database structure changes

❓ Why do we accept DTO in @RequestBody?
✅ Answer:
Because DTO defines the API contract and protects internal domain models from client manipulation.

❓ Why is DTO placed in Service layer and not Repository?
✅ Answer:
Repositories deal only with entities and persistence, while DTOs are used for business logic and data transfer between client and server.
DTOs belong to the API and business layers, while repositories operate strictly on persistence models (Entities). Mixing DTOs with repositories violates separation of concerns.

❓ Can we use Entity directly for small projects?
✅ Answer:
Yes, but it violates clean architecture principles and becomes hard to maintain as the application grows.

---

🗓 DAY 3 — Jackson, DTO Design & Deserialization

❓ How does Spring convert JSON to Java objects?
✅ Answer:
Spring uses Jackson's ObjectMapper internally to deserialize JSON into Java objects based on property names and types.

❓ What is @RequestBody?
✅ Answer:
It binds the HTTP request body to a Java object using HTTP message converters, automatically parsing JSON/XML to Java objects.

❓ Why do DTOs require a no-args constructor?
✅ Answer:
Jackson uses reflection to instantiate objects and requires a default constructor unless configured otherwise with custom deserialization logic.

❓ Why setters are required in DTO?
✅ Answer:
Jackson populates object fields using setters or direct field access during deserialization of incoming JSON data.
Jackson requires either setters, public fields, or field access (via reflection). Setters are preferred for encapsulation and flexibility.

❓ Difference between primitive and wrapper types in DTO?
✅ Answer:
| Feature | Primitive | Wrapper |
|---------|-----------|---------|
| Null value | Cannot be null | Can be null |
| API safety | Causes deserialization errors | Safe for APIs |
| Default value | Has default value | null by default |

❓ Why should DTO fields use wrapper types?
✅ Answer:
Because incoming JSON may omit fields, and wrapper types allow null values without causing deserialization errors.

❓ What causes ClassNotFoundException vs NoClassDefFoundError?
✅ Answer:
- ClassNotFoundException: Class not found during dynamic loading at runtime
- NoClassDefFoundError: Class present at compile time but missing at runtime

❓ Why did we get JSON parse error for long id?
✅ Answer:
Because Jackson cannot assign null to primitive types when the client does not send the field. Use Long (wrapper) instead of long (primitive).

❓ Should client send id during create API?
✅ Answer:
No. ID should be generated by the database (auto-increment), not the client. Client should omit the id field during creation.

---

🗓 DAY 4 — Bean Validation & Global Exception Handling

❓ What does @Valid do?
✅ Answer:
@Valid triggers bean validation before the controller method is executed. If validation fails, a MethodArgumentNotValidException is thrown.

❓ Where does validation failure occur?
✅ Answer:
During argument resolution phase, before entering the controller method body. Spring intercepts the request at this stage.

❓ Why 400 and not 500?
✅ Answer:
Validation errors are client-side input errors, not server failures. HTTP 400 (Bad Request) indicates the client sent invalid data, while 500 (Internal Server Error) indicates a server-side failure.

❓ Difference between @ControllerAdvice and @ExceptionHandler?
✅ Answer:
- @ExceptionHandler: Handles exceptions locally within a specific controller
- @RestControllerAdvice: Applies exception handling globally across all controllers in the application

❓ Why not validate in service layer?
✅ Answer:
Validation is a boundary concern that should occur at the entry point of your application. The service layer assumes it receives valid input and focuses on business logic rather than input validation.
“Service layer validation is business validation; input validation belongs to the boundary (controller).”

❓ What is @RestControllerAdvice?
✅ Answer:
A composite annotation combining @ControllerAdvice with @ResponseBody. It centralizes exception handling across controllers and automatically serializes exception responses to JSON.

❓ Why use @NotBlank instead of @NotNull?
✅ Answer:
- @NotNull: Allows empty strings and whitespace
- @NotBlank: Rejects null, empty strings, and whitespace-only strings for better data quality

❓ What happens when validation fails?
✅ Answer:
Spring catches the exception, calls the appropriate @ExceptionHandler method, and returns a structured error response with HTTP 400 status and validation error details.



🗓 DAY 5 — REST APIs (GET, POST, PUT, DELETE) + PathVariable & RequestParam

❓ What is a REST API?
✅ Answer:
REST (Representational State Transfer) is an architectural style where resources are accessed using HTTP methods. A REST API is stateless, uses standard HTTP methods, and operates on resources rather than actions.

Example:
- GET /employees
- GET /employees/101
- POST /employees
- PUT /employees/101
- DELETE /employees/101

❓ Difference between HTTP Methods?
✅ Answer:
| Method | Purpose | Idempotent | Used for |
|--------|---------|------------|----------|
| GET | Fetch data | ✅ Yes | Read |
| POST | Create data | ❌ No | Create |
| PUT | Update entire resource | ✅ Yes | Update |
| PATCH | Partial update | ❌ No | Partial update |
| DELETE | Delete resource | ✅ Yes | Delete |

❓ What does idempotent mean?
✅ Answer:
An operation is idempotent if calling it multiple times produces the same result as calling it once. GET, PUT, and DELETE are idempotent; POST and PATCH are not.

❓ What is @PathVariable?
✅ Answer:
@PathVariable extracts variable values from the URL path. Example: In /employees/{id}, @PathVariable Long id captures the id value from the URL.

❓ What is @RequestParam?
✅ Answer:
@RequestParam extracts query parameters from the URL. Example: /employees?department=IT extracts the department parameter from the query string.

❓ Difference between @PathVariable and @RequestParam?
✅ Answer:
| Feature | @PathVariable | @RequestParam |
|---------|---------------|---------------|
| Location | URL path | Query string |
| Purpose | Resource identifier | Filtering/filtering |
| Required | Usually mandatory | Optional by default |
| Example | /employees/101 | /employees?department=IT |

❓ Why use PathVariable for ID and RequestParam for filters?
✅ Answer:
PathVariable identifies the specific resource (unique),  while RequestParam provides optional filtering or sorting criteria to query parameters.

❓ What is HTTP idempotency used for?
✅ Answer:
Idempotency ensures that retrying a request (due to network failures) does not cause unintended side effects. Safe methods like GET and PUT can be safely retried.

❓ Why is POST not idempotent?
✅ Answer:
Because calling POST multiple times creates multiple resources, each call produces a different result (new ID), making it non-idempotent.


❓ What HTTP status code should POST return?
✅ Answer:
HTTP 201 (Created) indicates successful resource creation. The response should include the created resource and its location header.

❓ What HTTP status code should GET return?
✅ Answer:
HTTP 200 (OK) indicates successful retrieval of requested data.

❓ What is request body?
✅ Answer:
Binds incoming JSON request body to a Java object using Jackson.
    @PostMapping("/employees")
    public EmployeeDto create(@RequestBody EmployeeDto dto)

❓ Why POST uses @RequestBody but GET doesn’t?
✅ Answer:
GET requests should not have body
POST sends data in body for creation

❓ Why do we use separate HTTP methods instead of one endpoint?
✅ Answer:
Because REST follows uniform interface constraints and improves clarity, caching, and semantics.

❓ Why PUT is idempotent?
✅ Answer:
Calling PUT multiple times with same data results in same state.

❓ Can GET have a request body?
✅ Answer:
Technically possible but not recommended and not supported by Spring.

❓ Why @RestController instead of @Controller?
✅ Answer:
@RestController combines @Controller + @ResponseBody.

❓ Why constructor injection is preferred?
✅ Answer:
Immutability
Better testability
Avoids null beans

❓ What is PATCH?

✅ Answer (Interview-ready):
PATCH is an HTTP method used to partially update a resource.
Unlike PUT, which replaces the entire resource, PATCH modifies only the specified fields.


🗓 DAY 6 — Spring Boot JPA & Database Integration

❓ What is JPA?
✅ Answer:
JPA (Java Persistence API) is a Java specification for managing relational data in applications using ORM (Object-Relational Mapping).
Maps Java objects → database tables
Allows CRUD operations without writing SQL manually
Spring Boot uses Spring Data JPA as its implementation (usually Hibernate by default).

❓ Difference between JPA and Hibernate
Feature	                    JPA	            Hibernate
Type	                Specification	Implementation of JPA
Vendor	                Standard API	Specific ORM library
Requirement	Needs       JPA provider	Provides full JPA + extra features
Portability	                High	    Less portable if using Hibernate-specific features

❓ What is an Entity?
✅ Answer:
A Java class annotated with @Entity representing a database table.
Fields map to table columns.
Requires a primary key annotated with @Id.

❓ What is a Repository?
✅ Answer:
Interface to handle database operations using JPA.
Extends Spring Data interfaces like JpaRepository or CrudRepository.
Spring Boot automatically generates the implementation at runtime.

❓ Why use Spring Data JPA?
✅ Answer:
Reduces boilerplate code
Provides ready-to-use methods: save(), findById(), findAll(), deleteById()
Supports derived queries (method names define SQL)
Integrates with transactions and paging/sorting easily

❓ Difference between CrudRepository and JpaRepository
Feature	                    CrudRepository	                    JpaRepository
CRUD methods	    ✅ save, findAll, findById, delete	      ✅ All + batch save, flush, pagination
Pagination & Sorting	    ❌ Limited	                      ✅ Full support
Custom Queries	            ✅ JPQL or derived	              ✅ JPQL, derived, native SQL


❓ What is @GeneratedValue?
✅ Answer:
Defines automatic ID generation strategy for primary key:
IDENTITY → Auto-increment (MySQL, PostgreSQL)
SEQUENCE → Uses DB sequence
AUTO → Spring picks default strategy
TABLE → Uses a table to simulate sequence


❓ Transaction Management
✅ Answer:
Spring Boot manages transactions automatically using @Transactional.
CRUD operations in JPA are transactional.
Ensures atomicity: all changes succeed or fail together.

❓ Why DTO instead of Entity in APIs?
✅ Answer is the same as Day 2/3:
Security: prevents exposing DB schema
Loose coupling: API evolves independently from DB
Better validation and mapping

❓ Why use JPA instead of JDBC?
✅ Answer:
JPA provides object-relational mapping, so you can work with objects instead of SQL queries.
Handles connection management, caching, lazy loading automatically.
Supports database portability without changing code (use same entities for MySQL, Postgres, etc.)
Reduces boilerplate like ResultSet parsing, PreparedStatement setup, exception handling.

❓ What is @GeneratedValue?
✅ Answer (additional details):
Can define strategy per database to optimize inserts.
Can combine with @SequenceGenerator or @TableGenerator for custom ID generation rules.
Supports UUID, custom sequences beyond numeric auto-increment.

❓ What does spring.jpa.hibernate.ddl-auto=update do?
✅ Answer:
Automatically updates database schema to match entity definitions.
Does not delete data; only adds or alters columns/tables.
Useful in development, but should not be used in production (risk of data loss).
Other options:
validate → checks schema matches entities, no changes
create → drops & recreates schema every startup
create-drop → same as create, drops schema on shutdown

❓ How does Spring Boot create repository implementation automatically?
✅ Answer:
Spring Data uses dynamic proxies to generate a class at runtime that implements your repository interface.
The generated class handles CRUD operations, query methods, pagination, and sorting.
Custom methods with derived queries (like findByName) are parsed by Spring, and the appropriate JPQL query is generated automatically.

❓ What is the advantage of service layer in JPA?
✅ Answer:
Encapsulates transaction boundaries using @Transactional.
Provides a place to add business logic, so repository stays focused on persistence only.
Makes code testable, as services can be mocked independently of DB.
Reduces tightly coupled controllers, promoting clean architecture.

❓ Difference between save() for new entity vs existing entity?
✅ Answer:
New entity (no ID assigned / null ID):
save() performs an INSERT into the database.
JPA sets the generated ID back on the entity.
Existing entity (ID present and managed):
save() performs an UPDATE.
Only changes in entity fields are persisted.
Key point: JPA decides INSERT vs UPDATE based on entity identity and persistence context.


🗓 DAY 7 — Spring Security: Basic Authentication
❓ What is Spring Security?
✅ Answer:
Spring Security is a framework that provides authentication, authorization, and protection against common attacks for Spring applications.
It secures:
Who you are → Authentication
What you can do → Authorization
It works as a filter chain in front of your Spring Boot controllers.

❓ What happens when Spring Security is added to a project?
✅ Answer:
The moment you add the spring-boot-starter-security dependency:
All endpoints become secured
A login form is auto-generated
A default user is created
Password is printed in logs
You must authenticate to access any endpoint
This is called secure by default.

❓ What is Authentication?
✅ Answer:
Authentication is the process of verifying who the user is.
Examples:
Username + Password
Token (JWT)
OAuth login
If identity is verified → user is authenticated.

❓ What is Authorization?
✅ Answer:
Authorization decides what an authenticated user is allowed to do.
Example:
Admin → can delete users
Normal user → can only view data
Authentication comes first, authorization comes after.

❓ What is Basic Authentication?
✅ Answer:
Basic Authentication is an HTTP-based authentication scheme where:
Username and password are sent in every request
Encoded using Base64
Header:
Authorization: Basic base64(username:password)
It is:
Simple
Stateless
Not secure without HTTPS


❓ Why is Basic Auth considered weak?
✅ Answer:
Because:
Credentials are sent on every request
Base64 is NOT encryption (easily decoded)
If HTTPS is not used, credentials can be stolen
So it is only used for:
Internal services
Prototypes
Learning security

❓ How does Spring Security process a request?
✅ Answer:
Spring Security uses a Filter Chain:
Request comes in
Security filters run
Authentication happens
Authorization happens
If allowed → controller is called
If not → 401 or 403 returned
Your controller never sees unauthorized requests.

❓ What is a Security Filter Chain?
✅ Answer:
A sequence of security filters that intercept every HTTP request before it reaches controllers.
It handles:
Login
Authentication
CSRF(Cross-Site Request Forgery) is a web security vulnerability where attackers trick authenticated users into performing unwanted actions (like changing passwords or making purchases) on a trusted website without their consent, by exploiting the browser's automatic sending of session cookies.
Authorization
Logout
Session management

❓ What is UserDetailsService?
✅ Answer:
It is the interface Spring Security uses to load user data.

Spring calls:
loadUserByUsername(username)

It must return:
Username
Password
Roles
Spring never talks directly to DB — it talks through this interface.

❓ What is AuthenticationManager?
✅ Answer:
It is the core component that:
Takes username + password
Uses UserDetailsService
Uses PasswordEncoder
Decides if login is valid
It is the brain of authentication.

❓ What is PasswordEncoder?
✅ Answer:
Used to hash and verify passwords.
Spring never stores plain passwords.
It compares hashed passwords.
Example:
BCryptPasswordEncoder

❓ What is the difference between 401 and 403?
✅ Answer:
Code	Meaning
401	Not authenticated (no login)
403	Authenticated but not allowed

❓ @Configuration
✅ Answer:
The @Configuration annotation is a core Spring annotation used to declare a class as a source of bean definitions for the Spring application context. 
It indicates that the class contains methods annotated with @Bean, which will be processed by the Spring container to generate beans and wire dependencies. 
This allows you to define your application's structure using Java instead of XML. 

❓ @EnableWebSecurity 
✅ Answer:
The @EnableWebSecurity annotation is a specialized Spring Security annotation. Its primary functions are: 
Enables Web Security: It activates Spring Security's web security support.
Sets up the Filter Chain: It sets up the FilterChainProxy, which is the core filter that intercepts incoming HTTP requests and applies the necessary security rules.
Imports Core Configuration: It imports the WebSecurityConfiguration class internally, which exports necessary beans and provides a foundation for security customization. 

🗓 DAY 8 — JWT Authentication 
❓ What is JWT?
✅ Answer (Interview-ready):
JWT (JSON Web Token) is a compact, URL-safe token used to securely transmit claims between client and server.
It is stateless, meaning the server does not store session data.

❓ Why JWT over Basic Authentication?
✅ Answer:
Basic Auth                                  	JWT
Credentials sent on every request	 |    Token sent instead
Stateful / less scalable	         |    Stateless & scalable
Not suitable for microservices	     |    Ideal for microservices
No expiration control	             |    Supports expiry & refresh


❓ What does “stateless authentication” mean?
✅ Answer:
The server does not store user session data.
Each request carries all authentication information inside the JWT.

❓ Structure of JWT?
✅ Answer:
JWT has 3 parts, separated by dots:
HEADER.PAYLOAD.SIGNATURE
Header → Algorithm & token type
Payload → Claims (username, roles, expiry)
Signature → Verifies token integrity

❓ How JWT authentication flow works?
✅ Answer:
Client sends username + password to /login
Server validates credentials
Server generates JWT
Client stores JWT (header/local storage)
Client sends JWT in Authorization header
Server validates JWT for every request

❓ Where is JWT sent in request?
✅ Answer:
Authorization: Bearer <JWT_TOKEN>

❓ Why JWT is preferred in microservices?
✅ Answer:
No session replication
Works across services
Lightweight
Scales horizontally

❓ Can JWT be revoked?
✅ Answer:
Not directly (stateless).
Solutions:
Short expiry
Token blacklist
Refresh tokens

Client
  |
  | 1️⃣ Login Request (POST /api/auth/login)
  |    Body: { "username": "Ajay", "password": "password" }
  v
Spring Boot Controller (AuthController)
  |
  | 2️⃣ Authenticate using AuthenticationManager
  v
AuthenticationManager
  |
  | 3️⃣ Calls UserDetailsService.loadUserByUsername()
  v
UserDetailsService (InMemory or DB)
  |
  | 4️⃣ Returns UserDetails (username, password, roles)
  v
AuthenticationManager
  |
  | 5️⃣ Verifies password using PasswordEncoder
  v
AuthController
  |
  | 6️⃣ Generate JWT token using JwtUtil
  v
Client
  |
  | 7️⃣ Store JWT token (LocalStorage/Session)
  v
Future Requests (GET /employees, etc.)
  |
  | 8️⃣ Attach JWT in Authorization header: Bearer <token>
  v
Spring Security Filter Chain
  |
  | 9️⃣ JwtFilter (OncePerRequestFilter)
  |    - Extract token from header
  |    - Validate token
  |    - Extract username
  |    - Load UserDetails from UserDetailsService
  |    - Set Authentication in SecurityContext
  v
SecurityContextHolder
  |
  | 🔟 Controller executed if authenticated
  |    If unauthorized → return 401/403
  v
Controller (e.g., EmployeeController)
  |
  | 11️⃣ Perform business logic, return response
  v
Client
  |
  | 12️⃣ Receives response



🗓 DAY 9 — API Versioning + HATEOAS

❓ Why is API versioning needed?
✅ Answer:
API versioning is required to maintain backward compatibility. When you update an API, existing clients may break if the old endpoints are modified. Versioning ensures:
Existing clients continue to work
New features can be added safely
Deprecation of old functionality can be handled gradually

❓ What are the common API versioning strategies?
✅ Answer:
URI Versioning
Example: /api/v1/employees
Version is part of the URL
Pros: Simple, easy to test
Cons: URL changes for every version

Request Parameter Versioning
Example: /employees?version=1
Pros: Flexible, no URL changes
Cons: Not cache-friendly, less intuitive

Header Versioning
Example: X-API-VERSION: 1
Pros: Clean URL, no query params
Cons: Harder to test via browser

Media Type / Accept Header Versioning
Example: Accept: application/vnd.company.v1+json
Pros: Pure REST approach
Cons: Complex, rarely used

❓ Which versioning approach is most commonly used?
✅ Answer:
URI Versioning is most common in real-world projects.
Header and media type versioning are more REST-pure but less practical.
Interviews often ask both: pros/cons and “how to implement in Spring Boot.”

❓ How do you implement API versioning in Spring Boot?
✅ Answer:
URI versioning: Define separate endpoints per version in the controller.
@GetMapping("/api/v1/employees")
public List<EmployeeDto> getEmployeesV1() { ... }
@GetMapping("/api/v2/employees")
public List<EmployeeDto> getEmployeesV2() { ... }


Header versioning: Use @RequestMapping with headers attribute.
@GetMapping(value = "/employees", headers = "X-API-VERSION=1")
public List<EmployeeDto> getEmployeesV1() { ... }

Media type versioning: Use produces attribute.
@GetMapping(value = "/employees", produces = "application/vnd.company.v1+json")
public List<EmployeeDto> getEmployeesV1() { ... }


HATEOAS (Hypermedia as the Engine of Application State)
❓ What is HATEOAS?
✅ Answer:
HATEOAS is a REST principle where the API response contains links to related resources.
It allows clients to navigate the API dynamically
Reduces hard-coded endpoint knowledge on the client
Example:
{
  "id": 101,
  "name": "Ajay",
  "_links": {
    "self": { "href": "/api/v1/employees/101" },
    "all-employees": { "href": "/api/v1/employees" }
  }
}


❓ Why HATEOAS is rarely used but often asked in interviews?
✅ Answer:
Rare in real-world projects because: extra overhead, most clients know endpoints, simpler REST works fine
Popular in interviews to test understanding of REST maturity levels

❓ What are the benefits of HATEOAS?
✅ Answer:
Clients don’t hardcode URLs
API can evolve independently
Supports discoverability of resources
Aligns with Richardson Maturity Level 3 (fully RESTful)

❓ How do you implement HATEOAS in Spring Boot?
✅ Answer:
Use spring-boot-starter-hateoas dependency
Wrap responses in EntityModel or CollectionModel
Add links using WebMvcLinkBuilder

Example:
@GetMapping("/employees/{id}")
public EntityModel<EmployeeDto> getEmployee(@PathVariable Long id) {
    EmployeeDto employee = service.findById(id);
    return EntityModel.of(employee,
        linkTo(methodOn(EmployeeController.class).getEmployee(id)).withSelfRel(),
        linkTo(methodOn(EmployeeController.class).getAllEmployees()).withRel("all-employees"));
}


❓ Difference between normal REST and HATEOAS REST?
✅ Answer:
Feature	                Normal REST	                HATEOAS REST
Links	            No links in response	    Response contains navigable links
Client knowledge	Hardcoded endpoints	        Clients discover endpoints dynamically
REST maturity	        Level 2	                Level 3 (Richardson Maturity Model)

❓ Interview tip for HATEOAS:
✅ Answer:
Explain the principle, give JSON example, mention Spring HATEOAS classes like EntityModel, CollectionModel, WebMvcLinkBuilder.
Highlight that it improves discoverability and aligns with Level 3 REST.

📅 DAY 10 — MySQL + JPQL + Native Queries
❓ Why do we use MySQL with Spring Boot instead of H2?
✅ Answer:
MySQL is production-grade RDBMS, H2 is in-memory mainly for testing/dev.

❓ What properties are required to connect Spring Boot to MySQL?
✅ Answer:
spring.datasource.url
spring.datasource.username
spring.datasource.password
spring.jpa.hibernate.ddl-auto
spring.jpa.database-platform

❓ What does spring.jpa.hibernate.ddl-auto do?
✅ Answer:
Controls DB schema generation (none, update, create, create-drop).

❓ What is JPQL?
✅ Answer:
Object-oriented query language that works on entities and fields, not tables.

❓ Difference between JPQL and SQL?
✅ Answer:
JPQL works on entities, SQL works on tables.

❓ Example of JPQL query?
✅ Answer:
@Query("SELECT e FROM Employee e WHERE e.department = :dept")
List<Employee> findByDepartment(String dept);

❓ Can JPQL perform joins?
✅ Answer:
Yes.

❓ Does JPQL support aggregation functions?
✅ Answer:
Yes (COUNT, SUM, AVG, MIN, MAX).

❓ What is a native query?
✅ Answer:
DB-specific SQL query executed directly on database.

❓ When should native queries be used?
✅ Answer:
Complex queries, DB-specific features, performance optimization.

❓ Example of native query?
✅ Answer:
@Query(value = "SELECT * FROM employees WHERE department = ?1", nativeQuery = true)
List<Employee> findByDeptNative(String dept);

❓ Which annotation is used for custom queries?
✅ Answer:
@Query

❓ Difference between positional and named parameters?
✅ Answer:
Positional uses ?1, named uses :name.

❓ Can JPQL and native queries be used in same repository?
✅ Answer:
Yes.

❓ What is projection in JPA?
✅ Answer:
Fetching partial data instead of full entity.

❓ What is DTO mapping in queries?
✅ Answer:
Mapping query result directly to DTO object.

❓ What happens if DB column names don’t match entity fields in native query?
✅ Answer:
Mapping fails unless @Column(name="") or projection/DTO mapping is used.

❓ Is JPQL database independent?
✅ Answer:
Yes.

❓ Is native query database independent?
✅ Answer:
No.

❓ Which is preferred for portability?
✅ Answer:
JPQL.

❓ Which is preferred for performance tuning?
✅ Answer:
Native Query.

❓ What is JPQL?
✅ Answer:
JPQL is an object-oriented query language used in JPA.
It works on Entity objects and their fields, not on database tables or columns.

👉 JPQL is database-independent.

❓ What does JPQL query over?
✅ Answer:
JPQL queries:
Entities (Java classes)
Entity fields (Java properties)
❌ It does NOT use:
Table names
Column names
❓ Basic JPQL example – fetch all employees
✅ Answer:

@Query("SELECT e FROM Employee e")
List<Employee> findAllEmployees();
✔ Employee → Entity name
✔ e → alias
✔ returns managed entities

❓ JPQL example with WHERE condition
✅ Answer:

@Query("SELECT e FROM Employee e WHERE e.department = :dept")
List<Employee> findByDepartment(@Param("dept") String dept);
✔ Uses entity field names
✔ Supports named parameters

❓ JPQL example using method name vs @Query
✅ Answer:
Method name:
List<Employee> findByDepartment(String department);

JPQL:
@Query("SELECT e FROM Employee e WHERE e.department = :department")
List<Employee> fetchByDepartment(@Param("department") String department);


👉 Both generate JPQL internally

❓ JPQL example with projection (DTO)
✅ Answer:
@Query("SELECT new com.example.dto.EmployeeDto(e.id, e.name) FROM Employee e")
List<EmployeeDto> fetchEmployeeDtos();
✔ Avoids sending full entity
✔ Faster & API-safe

❓ Is JPQL translated to SQL?
✅ Answer:
Yes.
Hibernate converts JPQL → SQL based on the database dialect (MySQL, Postgres, etc.).

4️⃣ Native SQL Queries

❓ What is a Native Query?
✅ Answer:
A native query is pure SQL, written exactly as it would run in the database.

👉 Uses table names and column names

❓ Native query example
✅ Answer:
@Query(value = "SELECT * FROM employee WHERE department = ?1", nativeQuery = true)
List<Employee> findByDepartmentNative(String department);
✔ Uses table name employee
✔ Uses column department
✔ nativeQuery = true is mandatory

❓ Native query with named parameters
✅ Answer:
@Query(
  value = "SELECT * FROM employee WHERE salary > :salary",
  nativeQuery = true
)
List<Employee> findHighSalary(@Param("salary") double salary);


❓ Native query returning partial columns
✅ Answer:
@Query(
  value = "SELECT name, department FROM employee",
  nativeQuery = true
)
List<Object[]> fetchNameAndDepartment();

⚠ Requires manual mapping
⚠ Less type-safe

❓ Can native queries return DTO directly?
✅ Answer:
Yes, but requires:

Interface-based projection or

@SqlResultSetMapping (advanced)

👉 JPQL is simpler for DTOs

❓ Why native queries are faster sometimes?
✅ Answer:
Because:

No ORM abstraction

Direct DB execution

Can use DB-specific optimizations

5️⃣ JPQL vs Native Query (Interview Gold)

❓ JPQL vs Native SQL — key difference
✅ Answer:
Feature	           JPQL	      Native SQL
Works on	       Entities	    Tables
DB portability	  ✅ High	    ❌ Low
Compile-time safety	✅ Better 	❌ Less
ORM features	    ✅ Yes	      ❌ Limited
Performance tuning	❌ Limited	✅ Full

❓ When should you use JPQL?
✅ Answer:
Use JPQL when:
Query is entity-centric
Database portability matters
You want cleaner, safer code
DTO projections are required
👉 Default choice

❓ When should you use Native SQL?
✅ Answer:
Use Native queries when:
Complex joins / window functions
Vendor-specific SQL
Performance-critical queries
Legacy database queries

❓ Interview question:
“Which one do you prefer — JPQL or Native?”
✅ Answer:
“I prefer JPQL by default for portability and maintainability.
I use **native queries only when JPQL cannot express the required logic or for performance-critical cases.”

❓ Does Spring Data JPA encourage native queries?
✅ Answer:
No.
Spring Data JPA encourages:
1️⃣ Derived queries
2️⃣ JPQL
3️⃣ Native queries (last option)

❓ Can JPQL replace all SQL?
✅ Answer:
No.
JPQL cannot handle:
DB-specific functions
Advanced analytics queries
Stored procedures efficiently

📁 PROJECT STRUCTURE (Correct Design)
service
 ├── EmployeeService.java        <-- INTERFACE
 └── impl
     └── EmployeeServiceImpl.java <-- IMPLEMENTATION

🔵 EmployeeService (INTERFACE)

📄 EmployeeService.java
public interface EmployeeService {
    List<EmployeeDto> getAll();
    EmployeeDto getById(Long id);
    EmployeeDto create(EmployeeDto dto);
}


🔹 NO @Service here
🔹 NO business logic
🔹 Just contract

🟢 EmployeeServiceImpl (IMPLEMENTATION)
📄 EmployeeServiceImpl.java

@Service
public class EmployeeServiceImpl implements EmployeeService {

    private final EmployeeRepository repository;

    public EmployeeServiceImpl(EmployeeRepository repository) {
        this.repository = repository;
    }

    @Override
    public List<EmployeeDto> getAll() {
        return repository.findAll();
    }

    @Override
    public EmployeeDto getById(Long id) {
        return repository.findById(id)
                .orElseThrow(() -> new RuntimeException("Not found"));
    }
}


🔹 @Service goes ONLY here
🔹 Spring registers this class as a bean

🔴 WHY CONTROLLER IMPORTS INTERFACE?

📄 EmployeeController.java

@RestController
public class EmployeeController {

    private final EmployeeService employeeService;

    public EmployeeController(EmployeeService employeeService) {
        this.employeeService = employeeService;
    }
}

❓ Why not EmployeeServiceImpl?
Because:
You don’t care HOW it works
You care WHAT it provides
Spring does:
EmployeeService  --->  EmployeeServiceImpl
automatically.

🧩 HOW SPRING RESOLVES THIS?
Controller asks for EmployeeService
Spring scans beans
Finds:
EmployeeServiceImpl implements EmployeeService

Injects it automatically
🔥 This is Dependency Injection
⚠️ WHAT IF MULTIPLE IMPLEMENTATIONS EXIST?

Example:
EmployeeServiceImpl
EmployeeServiceMockImpl


Then Spring gets confused.
✅ Solution:
@Primary
@Service
public class EmployeeServiceImpl implements EmployeeService


OR

@Autowired
@Qualifier("employeeServiceImpl")
EmployeeService service;

🎯 INTERVIEW READY ANSWER

Q: Why inject interface instead of implementation?
✅ Loose coupling
✅ Easy testing (Mockito)
✅ Swap implementations easily
✅ Follows SOLID principles

Q: Who decides which implementation is used?
✅ Spring IoC Container


📅 DAY 11 — Unit Testing (JUnit 5 + Mockito)
❓ What is unit testing?
✅ Testing individual units (methods/classes) in isolation without external dependencies.

❓ What is the difference between unit test and integration test?
✅ Unit test: tests a single class using mocks
✅ Integration test: loads Spring context and real beans

❓ Why do we use Mockito?
✅ To mock dependencies and isolate the class under test.

❓ What is @Mock?
✅ Creates a mock object for a dependency.

❓ What is @InjectMocks?
✅ Injects mocked dependencies into the class under test.

❓ What is @ExtendWith(MockitoExtension.class)?
✅ Enables Mockito annotations in JUnit 5.

❓ Difference between @MockBean and @Mock?
✅ @Mock → pure unit tests (no Spring context)
✅ @MockBean → Spring Boot tests (replaces bean in context)

❓ What is stubbing?
✅ Defining behavior of a mock (when(...).thenReturn(...))

❓ Why should repositories be mocked in service tests?
✅ To avoid database dependency and keep tests fast and isolated.

❓ Should controllers be unit-tested?
✅ Rarely. Controllers are better tested using integration tests (MockMvc).


📅 DAY 12 — Integration Testing 

❓ 1. What is Integration Testing in Spring Boot?
✅ Answer:
Integration Testing checks the interaction between multiple layers (controller → service → repository → database).
Runs with the full Spring context.
Ensures that all components work together as expected, not just in isolation.
Unlike unit tests, it tests the actual wiring and dependencies, not mocks (except external services).

❓ 2. Why do we need MockMvc in Spring Boot tests?
✅ Answer:
MockMvc simulates HTTP requests to your controllers without starting a server.
Allows testing of REST endpoints (GET, POST, PUT, DELETE, PATCH).
Lets you validate HTTP status codes, response body, headers, and JSON structure.
Enables testing controller logic and request/response handling in isolation from the network.

❓ 3. What is the difference between Unit Testing and Integration Testing?
✅ Answer:

Feature	          Unit Testing	                   Integration Testing
Scope	        Single class or method	              Multiple layers together
Dependencies	    Mocked	                        Real (Spring context)
Purpose	Verify correctness of a method/class	    Verify system components work together
Execution	        Fast	                          Slower (loads Spring context)
Example	Testing .create() in isolationTesting     /employees endpoint end-to-end


❓ 4. How do you test REST APIs in Spring Boot without starting the server?
✅ Answer:
Use MockMvc provided by Spring Boot testing framework.
Configure it with @SpringBootTest and @AutoConfigureMockMvc.
Perform requests using mockMvc.perform(get("/url")) or post("/url").
No need to run the actual Tomcat/Jetty server.

❓ 5. How do you test HTTP status codes, response JSON, and error handling in Spring Boot?
✅ Answer:
Use MockMvc andExpect() methods:
.andExpect(status().isOk()) → checks HTTP 200
.andExpect(jsonPath("$.id").value(1)) → checks JSON field
.andExpect(jsonPath("$.name").value("Ajay")) → checks another field

For validation or exceptions:
.andExpect(status().isBadRequest()) → validation failure
.andExpect(jsonPath("$.error").value("Invalid data")) → exception message

❓ 6. What annotations are used to configure integration tests with Spring Boot and MockMvc?
✅ Answer:
@SpringBootTest → Loads the full Spring context.
@AutoConfigureMockMvc → Configures MockMvc automatically.
@ExtendWith(MockitoExtension.class) → Only for Mockito unit tests. Not needed for full integration tests.
@Transactional → Optional, to rollback DB changes after each test.
@Test → Marks the test method.


📅 Day 13 — Actuator + Custom Health Indicators

❓ 1. What is Spring Boot Actuator?
✅ Answer:
Spring Boot Actuator provides production-ready features like:
Health checks
Metrics
Application info
Monitoring endpoints
It helps in observing and managing applications in real time.

❓ 2. What are some commonly used Actuator endpoints?
✅ Answer:
/actuator/health → application health
/actuator/info → application info
/actuator/metrics → performance metrics
/actuator/env → environment properties
/actuator/beans → Spring beans list

❓ 3. How do you enable Actuator endpoints in Spring Boot?
✅ Answer:
Add dependency:
spring-boot-starter-actuator

Expose endpoints in application.yml or application.properties:
management.endpoints.web.exposure.include=health,info,metrics

❓ 4. What is the default health status returned by Actuator?
✅ Answer:
UP → application is healthy
DOWN → application is unhealthy
OUT_OF_SERVICE → temporarily unavailable
UNKNOWN → health cannot be determined

❓ 5. How do you customize /actuator/info endpoint?
✅ Answer:
Add properties:
info.app.name=Employee Service
info.app.version=1.0.0
info.app.description=Employee Management API

Then access:
/actuator/info

❓ 6. What is a custom Health Indicator in Spring Boot?
✅ Answer:
A custom Health Indicator allows you to define application-specific health checks
(e.g. database, external API, cache).

❓ 7. How do you create a custom Health Indicator?
✅ Answer:
Implement HealthIndicator:

@Component
public class DatabaseHealthIndicator implements HealthIndicator {
    @Override
    public Health health() {
        return Health.up()
                .withDetail("database", "MySQL is reachable")
                .build();
    }
}

❓ 8. How does a custom Health Indicator appear in /actuator/health?
✅ Answer:
It appears under components section:

{
  "status": "UP",
  "components": {
    "database": {
      "status": "UP",
      "details": {
        "database": "MySQL is reachable"
      }
    }
  }
}

❓ 9. How do you restrict Actuator endpoints in production?
✅ Answer:
Expose only required endpoints:
management.endpoints.web.exposure.include=health,info
management.endpoints.web.exposure.exclude=env,beans

❓ 10. Why is Actuator important in microservices?
✅ Answer:
Enables health checks for service discovery
Helps monitoring and alerting
Supports readiness and liveness probes
Critical for Kubernetes and cloud deployments


📅 Day 14 | Spring Profiles & Environment-based Beans

❓ What do Spring Profiles allow you to do?
✅ Spring Profiles allow you to:
Run the same codebase in different environments
Change configs, beans, databases, and features per environment
Avoid if–else hacks inside business code

👉 Common profiles:
dev
test
prod

❓ How does Spring Boot load profile-based configuration?
✅ Spring Boot:
Loads application.properties first
Overrides it with application-{profile}.properties
Merges configurations automatically

❓ How do you activate a Spring Profile?
✅ You can activate a profile using:
application.properties
JVM argument
Environment variable
Command line argument

❓ Example ways to activate a profile?
✅
spring.profiles.active=dev
-Dspring.profiles.active=prod
--spring.profiles.active=test


❓ What is the @Profile annotation used for?
✅ @Profile is used to:
Load beans only for specific environments
Keep environment-specific beans separate
Prevent unnecessary beans from loading

❓ Where can @Profile be applied?
✅ @Profile can be applied on:
@Configuration classes
@Component classes
@Bean methods

❓ How do you define multiple profiles for a bean?
✅ Use:
@Profile({"dev","test"})
👉 Bean loads if any one profile is active.

❓ What happens if no profile is active?
✅ Spring:
Uses the default profile
Loads all beans without @Profile annotation

❓ What is spring.profiles.default?
✅ It:
Acts as a fallback profile
Is used only when no active profile is set

❓ How are Spring Profiles used with databases?
✅ Common pattern:
dev → H2 / local DB
test → Test DB
prod → Production DB


❓ How do profiles help in CI/CD pipelines?
✅ Profiles allow:
Same JAR to be deployed everywhere
Environment behavior controlled externally
Safer and repeatable deployments

❓ Can profiles be changed at runtime?
✅ ❌ No
👉 Profiles are evaluated only at application startup

❓ Common mistake with Spring Profiles?
✅
Writing environment checks in code
Not using profiles for DB and security configs
Committing prod configs to Git


📅 Day 15 | Spring Cloud – Core Concepts

❓ 1. What is Spring Cloud?
✅ Answer:
Spring Cloud is a framework that provides tools to build distributed systems and microservices easily.
It solves problems like:
Service discovery
Configuration management
Load balancing
Fault tolerance
API gateway


❓ 2. Why do we need Spring Cloud when we already have Spring Boot?
✅ Answer:
Spring Boot helps build single services, but Spring Cloud helps manage multiple services.
👉 Spring Cloud adds:
Centralized config
Inter-service communication
Resilience
Dynamic scaling support

❓ 3. What is Service Discovery?
✅ Answer:
Service Discovery allows services to find each other dynamically without hardcoding IPs or ports.
👉 Instead of:
http://localhost:8081

We use:
http://employee-service

/*
A service registry is a centralized, highly available database or directory used in microservices architecture 
to track service instances, their network locations (IP addresses and ports), and metadata.

Service discovery is the automated process of detecting devices, services, or network locations (IP addresses and ports)
in a distributed system, such as microservices, to enable seamless communication.
*/
❓ 4. What is Eureka?
✅ Answer:
Eureka is a Service Registry provided by Spring Cloud Netflix.
It:
Registers services at startup
Keeps track of live instances
Allows clients to discover services dynamically

❓ 5. Difference between Client-side and Server-side Service Discovery?
✅ Answer:
Client-side (Eureka):
Client asks registry
Client chooses instance
More control, more responsibility

Server-side (Kubernetes):
Load balancer handles discovery
Client unaware of instances


❓ 6. What is Spring Cloud Config?
✅ Answer:
Spring Cloud Config provides centralized configuration management.
👉 Configs are stored in:
Git
File system
Vault
👉 All services fetch configs at runtime.

❓ 7. Why centralized configuration is important?
✅ Answer:
Centralized config helps to:
Avoid duplicated configs
Change config without redeploying
Maintain environment-specific values
Improve security & consistency

❓ 8. What is an API Gateway?

✅ Answer:
An API Gateway is a single entry point for all clients.
It handles:
Routing
Authentication
Rate limiting
Logging
Load balancing

❓ 9. What is Spring Cloud Gateway?
✅ Answer:
Spring Cloud Gateway is a non-blocking API Gateway built on Spring WebFlux.
It supports:
Dynamic routing
Filters (pre/post)
JWT validation
Path rewriting

❓ 10. What is Load Balancing in Spring Cloud?
✅ Answer:
Load balancing distributes traffic across multiple instances of a service.
Spring Cloud uses:
Spring Cloud LoadBalancer
Earlier: Ribbon (now deprecated)

❓ 11. What is Circuit Breaker?
✅ Answer:
A Circuit Breaker prevents cascading failures when a service is down.
It:
Stops calling failing services
Returns fallback response
Recovers automatically
Common implementation:
👉 Resilience4j

❓ 12. What is Fault Tolerance?
✅ Answer:
Fault tolerance ensures system stability when:
A service crashes
Network latency increases
Dependencies fail

Achieved using:
Circuit breakers
Timeouts
Retries
Bulkheads

❓ 13. What is Distributed Tracing?
✅ Answer:
Distributed tracing tracks a request across multiple microservices.
It helps:
Debug latency issues
Trace failures
Understand call flow

Tools:
Zipkin
Sleuth
OpenTelemetry

❓ 15. What are common Spring Cloud components?
✅ Answer:
Eureka (Service Discovery)
Config Server (Centralized config)
Gateway (API Gateway)
LoadBalancer
Resilience4j
Sleuth / Zipkin


📅 Day 16 — Eureka Server & Service Discovery 

           ┌───────────────────┐
           │   Eureka Server   │  (8761)
           └─────────▲─────────┘
                     │
     registers        │        registers
                     │
┌────────────────────┴──────┐   ┌─────────────────────────┐
│   employee-service (8080) │   │ department-service (8081)│
└───────────────┬───────────┘   └───────────────▲─────────┘
                │   service-name call             │
                └──────────► department-service ◄┘

❓ What is Eureka?
✅ Answer:
Eureka is a service registry used in microservices architecture where services register themselves and discover other services dynamically.

❓ Why do we need Service Discovery in Microservices?
✅ Answer:
Services scale dynamically
IP addresses and ports change frequently
Avoids hard-coding service URLs
Enables load balancing and fault tolerance

❓ What is Eureka Server?
✅ Answer:
Eureka Server acts as a registry that stores information about all registered microservices.

❓ What is Eureka Client?
✅ Answer:
A Eureka Client is a microservice that:
Registers itself with Eureka Server
Discovers other services from Eureka

❓ What problems does Eureka solve?
✅ Answer:
Dynamic service discovery
Load balancing between instances
Loose coupling between services
High availability

❓ How does Service Registration work in Eureka?
✅ Answer:
Service starts
Registers with Eureka Server
Sends periodic heartbeat signals
Eureka keeps service metadata

❓ What is Service Discovery?
✅ Answer:
It is the process by which a microservice finds the location of another service using the service registry.

❓ What is a Heartbeat in Eureka?
✅ Answer:
Heartbeat is a periodic signal sent by a service to Eureka Server to indicate that it is alive.

❓ What happens if a service stops sending heartbeats?
✅ Answer:
Eureka removes the service instance from the registry.

❓ What annotation is used to enable Eureka Server?
✅ Answer:
@EnableEurekaServer

❓ What annotation is used to enable Eureka Client?
✅ Answer:
@EnableEurekaClient

❓ What is Client-Side Service Discovery?
✅ Answer:
In client-side discovery:
Client queries Eureka
Client chooses an instance
Load balancing happens on client side

❓ How does Spring Cloud LoadBalancer work with Eureka?
✅ Answer:
It selects one instance from the available registered service instances.

❓ How does Feign Client use Eureka?
✅ Answer:
Feign uses service name instead of URL to make REST calls.

❓ What happens if Eureka Server goes down?
✅ Answer:
Already running services continue using cached registry
New services cannot register
Discovery may fail over time

❓ On which port does Eureka Dashboard run by default?
✅ Answer:
8761

❓ Can Eureka run on localhost?
✅ Answer:
Yes, Eureka works perfectly on localhost for development and learning.


🗓️ Day 17 — Spring Cloud Gateway

❓ What is Spring Cloud Gateway?
✅ Answer:
Spring Cloud Gateway is an API Gateway solution provided by Spring Cloud that acts as a single entry point for all microservices.
It routes client requests to appropriate backend services and supports load balancing, security, and monitoring.

❓ Why do we need an API Gateway in microservices?
✅ Answer:
Provides a single entry point for all services
Hides internal service URLs from clients
Enables centralized authentication and authorization
Supports load balancing and fault tolerance
Simplifies client-side logic

❓ How does Spring Cloud Gateway work?
✅ Answer:
Spring Cloud Gateway intercepts incoming HTTP requests, matches them against configured routes, applies filters if required, and forwards them to backend services registered with Eureka or defined via URLs.

❓ Does Spring Cloud Gateway use Spring MVC?
✅ Answer:
No. Spring Cloud Gateway is built on Spring WebFlux, which is non-blocking and reactive.

designed to handle high concurrency with fewer hardware resources by moving away from the traditional thread-per-request model. It is an alternative to the classic Spring MVC and is built on the Project Reactor library, which implements the Reactive Streams specification. 
Key Features and Concepts
Non-blocking I/O: 
WebFlux uses an event-loop execution model (like Node.js) with a small, fixed-size thread pool. When an operation (e.g., a database call or remote service call) would typically block a thread in Spring MVC, WebFlux instead registers a callback and frees the thread to process other requests, significantly improving scalability for I/O-bound applications.

Reactive Streams: 
It is built on the Reactive Streams specification, ensuring efficient data flow and backpressure (a mechanism for the consumer to signal the producer to slow down the data stream to prevent being overwhelmed).

Mono and Flux: 
The core types for handling asynchronous data streams are Mono and Flux.
Mono: Represents a publisher that emits zero or one element.
Flux: Represents a publisher that emits zero to N elements (a stream of data).

Programming Models: 
WebFlux offers two ways to build applications:
Annotated Controllers: 
Uses familiar Spring MVC-style annotations (@RestController, @GetMapping, etc.) with the key difference that controller methods return Mono or Flux types.
Functional Endpoints: 
A lightweight, functional programming model using lambda expressions to define routing (RouterFunctions) and request handling (HandlerFunctions).


❓ What is the role of Eureka in Spring Cloud Gateway?
✅ Answer:
Eureka helps Spring Cloud Gateway discover backend microservices dynamically using service names instead of hardcoded URLs.

❓ Does Spring Cloud Gateway need controllers?
✅ Answer:
No. Spring Cloud Gateway works using route definitions and filters; it does not require controllers.

❓ What happens if a service goes down?
✅ Answer:
If a service is unavailable, Eureka removes it from the registry and Spring Cloud Gateway stops routing traffic to that instance.


📅 Day 18 — Spring Cloud Config Server

❓ What is Spring Cloud Config Server?
✅ Answer:
Spring Cloud Config Server provides centralized external configuration for all microservices.
Instead of keeping application.yml inside every service, configurations are stored in a Git repository and fetched at runtime.

❓ Why do we need Spring Cloud Config Server?
✅ Answer:
It solves major problems in microservices like:
- Duplicate configuration across services
- Manual config changes and redeployments
- Environment-specific configs (dev, qa, prod)
- Secure handling of sensitive properties

❓ How does Spring Cloud Config work?
✅ Answer:
Config Server reads configuration from Git (or filesystem)
Microservices act as Config Clients
Clients fetch config on startup via HTTP
Configuration is applied before application context loads

❓ What are the main components of Spring Cloud Config?
✅ Answer:
Config Server – central configuration provider
Config Client – microservices consuming configuration
Git Repository – source of truth for configs

❓ What is the default URL pattern used by Config Server?
✅ Answer:
/{application-name}/{profile}
/{application-name}-{profile}.yml
Example:
http://localhost:8888/employee-service/dev

❓ Where does Config Server load properties before Spring Boot?
✅ Answer:
It loads properties in the bootstrap phase, before application.yml, ensuring external config has higher priority.

❓ What annotation enables Config Server?
✅ Answer:
@EnableConfigServer

❓ Can multiple services share the same configuration?

✅ Answer:
Yes. Common configs can be placed in:
application.yml
application-dev.yml
and shared across all services.


📅 Day 19 – Inter-Service Communication (RestTemplate & Feign)

❓ What is inter-service communication in microservices?
✅ Inter-service communication means one microservice calls another microservice over a network, usually using HTTP or messaging, to complete a business requirement that cannot be handled by a single service alone.

❓ Why is inter-service communication required in microservices architecture?
✅ Because in microservices each service has its own database and responsibility, so to build a complete feature like employee details with department information, services must talk to each other instead of directly accessing another service database.

❓ What are common ways to communicate between services in Spring Boot?
✅ The most common approaches are RestTemplate, OpenFeign, WebClient, and asynchronous communication using message brokers like Kafka or RabbitMQ.

❓ What is RestTemplate in Spring Boot?
✅ RestTemplate is a synchronous HTTP client provided by Spring that allows one service to call another service using methods like GET, POST, PUT, and DELETE through Java code.

❓ Why is RestTemplate considered old now?
✅ RestTemplate requires a lot of boilerplate code and manual handling of URLs and responses, so modern applications prefer OpenFeign or WebClient which are cleaner and declarative.

❓ What is OpenFeign?
✅ OpenFeign is a declarative REST client where we define only an interface with annotations, and Spring automatically creates the implementation to call another service.

❓ What is the main advantage of Feign over RestTemplate?
✅ Feign reduces boilerplate code, improves readability, supports service discovery, load balancing, and integrates easily with circuit breaker patterns.

❓ Is Feign synchronous or asynchronous by default?
✅ Feign is synchronous and blocking by default, meaning the calling thread waits for the response from another service.

❓ Can Feign work with Eureka and Resilience4j?
✅ Yes, Feign can directly use Eureka service names instead of URLs and can be combined with Resilience4j for retry and circuit breaker support.

❓ In our project which service will act as client?
✅ Our Employee Service will act as client and call another service to fetch additional information.


📅 Day 20 | Resilience4j (Circuit Breaker, Retry)
❓ 1. What is Resilience4j?
✅ Resilience4j is a lightweight fault tolerance library for Java and Spring Boot that provides patterns like Circuit Breaker, Retry, Rate Limiter and Bulkhead to handle failures in inter-service communication gracefully.

❓ 2. Why do we need Circuit Breaker in microservices?
✅ When one service like Department Service goes down, continuous calls from Employee Service will waste threads and increase latency, therefore Circuit Breaker stops real calls and returns a fallback response.

❓ 3. What are the states of Circuit Breaker?
✅ Circuit Breaker has three states: CLOSED where all calls are allowed, OPEN where all calls are blocked, and HALF-OPEN where limited test calls are allowed to check recovery.

❓ 4. What is Retry pattern?
✅ Retry pattern automatically calls the failed API again for a configured number of attempts before finally failing.

❓ 5. Difference between Retry and Circuit Breaker?
✅ Retry tries the same request again hoping temporary issue will be fixed, while Circuit Breaker completely stops calling the failing service for some time.

❓ 6. Can Resilience4j be used with Feign client?
✅ Yes Resilience4j integrates easily with Feign and RestTemplate in Spring Boot.

❓ 7. What is fallback method?
✅ Fallback is an alternate method that returns default response when circuit is open or all retries are failed.

❓ 8. Where do we configure Resilience4j?
✅ Configuration can be written in application.yml or application.properties with instance name.

❓ 9. Can Retry and Circuit Breaker be combined?
✅ Yes normally Retry happens first and if still failing Circuit Breaker opens.

❓ 10. Is Resilience4j blocking or non-blocking?
✅ Core library is functional and can work in both blocking and reactive environments.


📅 Day 21 – Distributed Tracing (Sleuth + Zipkin)
✅ Distributed Tracing is a technique to track a single user request as it travels across multiple microservices.
It assigns a unique Trace ID to the request and follows it through every service, database call, or HTTP call.

This helps developers understand:
- which services were called
- in what order
- how much time each service took
- where failure happened

Without tracing, logs of different services remain disconnected and debugging becomes very hard.


❓ What are Trace ID and Span ID?
✅ Trace ID → Unique ID for the entire request lifecycle across all services.
Span ID → Unique ID for each individual step/service inside that trace.

Example:
User calls Employee Service → it calls Department Service.
Trace ID: 789xyz
  ├── Span 1: Employee Service
  └── Span 2: Department Service

Both services will print same Trace ID in logs so we can correlate them.


❓ What is Spring Cloud Sleuth?
✅Spring Cloud Sleuth is a library that automatically:
- generates Trace ID & Span ID
- attaches them to logs
- propagates them in HTTP headers
- works with RestTemplate & Feign automatically
Developers don’t need to write manual tracing code.

❓ What is Zipkin?
✅ Zipkin is a tracing server with UI that:
- collects tracing data from services
- visualizes request flow
- shows latency per service
- helps find slow or failed services

Sleuth = generates tracing
Zipkin = displays tracing


❓ How Sleuth + Zipkin work together?
✅ User calls Employee Service
- Sleuth generates Trace ID
- Employee calls Department
- Same Trace ID is forwarded
- Both services send tracing data to Zipkin
- Zipkin UI shows full flow

❓ What is Sampling in tracing?
✅ Sampling means not sending 100% of traces to Zipkin.
To reduce load we can send:
- 10% of requests
- or all requests

Example:
spring.sleuth.sampler.probability=0.1

📅 Day 22 – Spring Cloud Bus
❓ What is Spring Cloud Bus?
✅ Spring Cloud Bus is a mechanism to broadcast events across multiple microservice instances using a message broker like RabbitMQ or Kafka.
It allows services to communicate configuration changes, refresh events, or custom events without calling each instance manually.

❓ Why do we need Spring Cloud Bus?
✅ When we use Spring Cloud Config Server, refreshing configuration requires calling:
POST /actuator/refresh
on every service instance separately.
Spring Cloud Bus solves this by:
- Sending one refresh event
- Broadcasting to all instances
- Keeping all services in sync automatically.

❓ Which message brokers are supported?
✅ Spring Cloud Bus supports:
- RabbitMQ (most common)
- Kafka
- Any Spring Cloud Stream compatible broker.

❓ How does Spring Cloud Bus work internally?
✅ A service publishes an event to the broker
Spring Cloud Bus listens to that broker
All subscribed services receive the event
Each service performs the action (e.g., refresh configuration).

❓ What are common use cases of Spring Cloud Bus?
✅ Refresh configuration across services
Clear cache in all instances
Broadcast custom domain events
Synchronize application state.

❓ What endpoint is used to trigger Bus refresh?
✅ POST /actuator/busrefresh
This triggers refresh for all services connected to the bus.

Advanced Message Queuing Protocol (AMQP) is an open-standard, binary application layer protocol designed for high-performance, reliable, and secure message-oriented middleware.
It facilitates interoperability between applications, platforms, and brokers (e.g., RabbitMQ) by handling routing, queuing, and reliable delivery with message guarantees, making it ideal for financial services, IoT, and distributed systems. 
Key Aspects of AMQP
Protocol Model: Utilizes a publish-subscribe model where producers send messages to exchanges, which then distribute them to queues based on defined rules (bindings).
Security & Reliability: Supports TLS for encryption and SASL for authentication, ensuring secure communication. It offers message delivery guarantees like at-most-once, at-least-once, and exactly-once.
Key Features: Enables efficient, interoperable messaging across different languages and operating systems, often used in RabbitMQ or as a standard for business messaging.
Versions: AMQP 1.0 is an OASIS and ISO/IEC 19464 standard. 

📅 Day 23 Kafka / RabbitMQ 
❓ Q1. What is a Message Broker and why do we need it?
✅ A message broker is middleware that enables asynchronous communication between microservices. Instead of calling another service directly using REST,
a service can publish a message to the broker and other services can consume it whenever they are available. This removes tight coupling, improves reliability, 
supports retry, and allows event-driven architecture where services react to events rather than waiting for synchronous responses.

❓ Q2. What is RabbitMQ?
✅ RabbitMQ is a message broker based on the AMQP protocol. It uses queues, exchanges, and bindings to route messages from producers to consumers. It is best suited for reliable task processing, background jobs, and scenarios where guaranteed delivery with acknowledgements is required.

❓ Q3. What is Kafka?
✅Kafka is a distributed event streaming platform that stores messages in topics as a log. It is designed for high throughput, fault tolerance, and real-time data streaming. 
Kafka retains messages for a configurable time and allows multiple consumers to read the same data independently.

❓ Q4. Difference between Kafka and RabbitMQ?
✅ Kafka is log-based and optimized for streaming large volumes of data with high throughput, while RabbitMQ is queue-based and optimized for reliable message delivery with complex routing. 
Kafka stores messages for a long time and supports replay, whereas RabbitMQ removes messages after consumption by default. Kafka is pull-based; RabbitMQ is push-based.

❓ Q5. What are core components of RabbitMQ?
✅ RabbitMQ has producers, exchanges, queues, bindings, and consumers. Producers send messages to an exchange. Exchange routes messages to queues based on routing rules. 
Consumers read messages from queues and acknowledge them after processing.

❓ Q6. What are core components of Kafka?
✅ Kafka contains producers, topics, partitions, brokers, consumers, and consumer groups. Producers publish messages to topics. Topics are divided into partitions for scalability. 
Consumers read messages using offsets, and consumer groups enable parallel processing.

❓ Q7. When should we use RabbitMQ?
✅RabbitMQ is preferred when we need complex routing, priority queues, guaranteed delivery, request-response patterns, and background job processing where each message must be processed exactly once.

❓ Q8. When should we use Kafka?
✅Kafka should be used for event streaming, log aggregation, analytics pipelines, real-time data processing, and scenarios where messages must be stored and replayed multiple times by different consumers.

❓ Q9. What is acknowledgment in RabbitMQ?
✅Acknowledgment is a signal sent by the consumer to RabbitMQ after successful processing of a message. If acknowledgment is not received, RabbitMQ requeues the message to avoid data loss.

❓ Q10. What is consumer group in Kafka?
✅A consumer group is a set of consumers that share the work of reading from Kafka topics. Each partition is read by only one consumer in a group, enabling parallel and scalable processing.

Controller → Producer → RabbitTemplate → Exchange → Queue → Consumer ✔


📅 Day 24 – Event-Driven Architecture (Spring Cloud Stream)
❓  Why Event-Driven?
✅ Instead of:
Employee Service → REST → Notification Service

We do:
Employee Service → EVENT → Kafka/RabbitMQ → Notification Service

Benefits
Loose coupling
Async processing
Retry & durability
Multiple consumers
Technology agnostic

❓  What is Spring Cloud Stream?
✅ It abstracts:
Kafka
RabbitMQ
Any broker

👉 You don’t use:
KafkaTemplate
RabbitTemplate
👉 You write only business logic as functions

❓ Q1. What is Event-Driven Architecture?
✅ Answer:
Event-Driven Architecture (EDA) is a design where:
Services communicate through events instead of direct REST calls.
One service publishes an event → other services consume it asynchronously.
Example:
Employee Created → Notification Service sends email.

❓ Q2. What is an Event?
✅  Answer:
An event is:
A record of something that has happened in the system
Immutable
Contains minimal business data
Example:

{
  "employeeId": "101",
  "action": "CREATED"
}

❓ Q3. Difference between REST vs Event-Driven?
✅ Answer:
REST	            Event Driven
Synchronous	      Asynchronous
Tight coupling  	Loose coupling
Caller waits	    Fire & forget
Hard to scale	    Highly scalable

❓ Q4. What is Spring Cloud Stream?
✅ Answer:
Spring Cloud Stream is:
Abstraction over Kafka / RabbitMQ
Developer writes only business logic
No KafkaTemplate or RabbitTemplate needed
Uses functional programming model

❓ Q5. What are Bindings?
✅ Answer:
Bindings connect:
Application code → Broker topic/queue
Defined in application.yml
Example: employeeEvent-out-0

❓ Q6. What is Consumer Group?
✅ Answer:
Multiple instances share same group
Each message processed by only one instance
Provides load balancing

❓ Q7. Advantages of Spring Cloud Stream?
✅ Answer:
Broker independent
Built-in retry
DLQ support
Easy scaling
Functional model